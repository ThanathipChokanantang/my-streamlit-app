import streamlit as st
import pandas as pd
import io
import json
import plotly.express as px
from google import genai
from google.genai import types

# --- 1. CONFIGURATION AND UTILITIES ---

# Schema ที่แก้ไขเพื่อให้ 'เนื้อหา_ข่าว_สรุป' เป็นพารากราฟยาว และเน้นการสกัดตัวเลข
JSON_SCHEMA = {
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id_ข่าว": {"type": "integer", "description": "รหัสข่าวที่อ้างอิง"},
            # แก้ไข: ให้ LLM สกัด Date/Time ที่ชัดเจนจากเนื้อหาข่าว
            "วันที่_และ_เวลา": {"type": "string", "description": "วันที่และเวลาที่ข่าวนี้ถูกรายงาน (เช่น YYYY-MM-DD HH:MM:SS) ต้องสกัดจากเนื้อหา"},
            "แหล่งที่มา_ของ_ข่าว": {"type": "string", "description": "ชื่อเว็บไซต์/แหล่งที่มาของข่าวสาร"},
            # แก้ไข: เปลี่ยนเป็นพารากราฟยาว 100-300 คำ
            "เนื้อหา_ข่าว_สรุป": {"type": "string", "description": "สรุปเนื้อหาข่าวที่มีรายละเอียดแตกต่างจากข่าวอื่น โดยมีความยาว 100 ถึง 300 คำ และเน้นข้อสรุปเป็นหลัก"},
            # เน้นการสกัดตัวเลขหลัก
            "ผู้เสียชีวิต_จำนวน": {"type": "integer", "description": "จำนวนผู้เสียชีวิตที่ระบุในข่าวนี้ (ต้องเป็นตัวเลขที่ชัดเจนและถูกต้องตามเนื้อหาเท่านั้น)"},
            "ผู้บาดเจ็บ_จำนวน": {"type": "integer", "description": "จำนวนผู้ได้รับบาดเจ็บที่ระบุในข่าวนี้ (ต้องเป็นตัวเลขที่ชัดเจนและถูกต้องตามเนื้อหาเท่านั้น)"}
        },
        # เน้นว่าตัวเลขต้องมี! ถ้าไม่มีต้องเปลี่ยนข่าว (LLM จะกรองข่าวที่มีตัวเลขให้)
        "required": ["id_ข่าว", "วันที่_และ_เวลา", "แหล่งที่มา_ของ_ข่าว", "เนื้อหา_ข่าว_สรุป", "ผู้เสียชีวิต_จำนวน", "ผู้บาดเจ็บ_จำนวน"]
    }
}

def create_summary_prompt(disaster_topic: str) -> tuple[str, str]:
    """สร้าง System และ User Prompt สำหรับการสกัดข้อมูลเชิงปริมาณ"""
    
    system_prompt = (
        "คุณคือผู้เชี่ยวชาญด้านการวิเคราะห์ข้อมูลภัยพิบัติ หน้าที่ของคุณคือการวิเคราะห์ชุดข้อความข่าวที่แตกต่างกัน "
        "ที่เกี่ยวข้องกับเหตุการณ์ '"+ disaster_topic +"' สกัดข้อมูลที่สำคัญตาม Schema JSON ที่กำหนดอย่างเคร่งครัด "
        "กฎหลักคือ: 1. ต้องสกัด 'วันที่_และ_เวลา' ออกจากเนื้อหาข่าวโดยใช้ NLP 2. 'ผู้เสียชีวิต_จำนวน' และ 'ผู้บาดเจ็บ_จำนวน' ต้องมีตัวเลขระบุชัดเจนในข่าว (ถ้าข่าวไหนไม่มีตัวเลข ให้ละทิ้งข่าวรายการนั้น) 3. 'เนื้อหา_ข่าว_สรุป' ต้องเป็นพารากราฟที่แตกต่างกันในแต่ละข่าว มีความยาว 100-300 คำ ผลลัพธ์จะต้องเป็น JSON array เท่านั้น ห้ามมีคำอธิบายอื่น ๆ"
    )
    
    user_prompt = f"""
    โปรดวิเคราะห์ชุดข้อความข่าวจำนวนมากที่ฉันจะนำเสนอให้คุณ ซึ่งเกี่ยวข้องกับเหตุการณ์ '{disaster_topic}' 
    และแปลงข้อมูลเหล่านั้นให้เป็นโครงสร้าง JSON array ที่สอดคล้องกับ Schema JSON ที่กำหนดอย่างเคร่งครัด 
    """
    
    return system_prompt, user_prompt

def get_placeholder_data(client: genai.Client, topic: str) -> str:
    """
    ใช้ Gemini API พร้อม Google Search Tool เพื่อค้นหาข้อมูลดิบ 
    และจำลองการมี 20 ข่าวสำหรับการวิเคราะห์
    """
    
    st.info(f"🔍 ขั้นตอนที่ 1: กำลังค้นหาข้อมูลจากเว็บเกี่ยวกับ '{topic}' โดยใช้ Google Search Tool...")
    
    try:
        # Prompt ที่สั่งให้ Gemini ค้นหาข่าวที่มีตัวเลข (เพื่อช่วยในการกรองข่าวตามโจทย์)
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=[f"ค้นหาข่าวสารและรายงานล่าสุดเกี่ยวกับเหตุการณ์ {topic} ที่มีข้อมูล **จำนวนผู้เสียชีวิต** และ **ผู้บาดเจ็บ** พร้อมวันที่และแหล่งที่มา"],
            config=types.GenerateContentConfig(
                tools=[{"google_search": {}}]
            )
        )
        
        # ดึงข้อมูลจาก Search Results (ใช้ Search Results จริง)
        if response.candidates and response.candidates[0].grounding_metadata and response.candidates[0].grounding_metadata.web_search_queries:
            
            search_queries = response.candidates[0].grounding_metadata.web_search_queries
            
            # จำลองการมี 20 ข่าว (ใช้ 10 Queries และทำซ้ำ)
            all_texts = []
            
            for i, query in enumerate(search_queries[:10]):
                # เนื่องจาก Gemini API ให้เพียง Query String เราต้องจำลอง URL/Source จาก Query
                source_name = f"Website Source {i+1}"
                
                # ข่าวจริง (1-10)
                all_texts.append(f"--- NEW NEWS ---\nID: {i+1}\nแหล่งที่มา: {source_name}\nเนื้อหา: {query}")
                
                # ข่าวจำลอง (11-20) - โดยสมมติว่ามีอัปเดตข้อมูลจำนวนผู้บาดเจ็บ/เสียชีวิตและแหล่งที่มาต่างกัน
                all_texts.append(f"--- NEW NEWS ---\nID: {i+11}\nแหล่งที่มา: Local Report {i+1}\nเนื้อหา: {query.replace('ผู้เสียชีวิต', 'ผู้เสียชีวิตเพิ่ม').replace('ผู้บาดเจ็บ', 'ผู้บาดเจ็บรวม')}")

            st.success("✅ รวบรวมข้อมูลดิบสำหรับการสกัดสำเร็จ (จำลอง 20 แหล่ง)")
            return "\n".join(all_texts)
        
        return "ไม่พบข้อมูลข่าวสารที่เกี่ยวข้อง"

    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดขณะเรียกใช้ Gemini Tool: {e}")
        return "ไม่พบข้อมูลข่าวสารที่เกี่ยวข้อง"

# --- 2. STREAMLIT APP LAYOUT ---

st.set_page_config(
    page_title="Disaster Event Summarizer (DES) 📊",
    layout="wide"
)

st.title("🌊 Disaster Event Summarizer (DES) and Analysis")
st.caption("รวบรวมข้อมูลภัยพิบัติจากหลายแหล่ง สกัดตัวเลข และสร้างกราฟวิเคราะห์ตามเวลา")

# --- Sidebar for API Key ---

with st.sidebar:
    st.header("🔑 Gemini API Configuration")
    
    api_key = st.text_input(
        "Enter your Gemini API Key", 
        type="password", 
        help="หาได้จาก Google AI Studio"
    )
    st.divider()
    st.info("💡 หมายเหตุ: แอปนี้ใช้ Google Search Tool ในการรวบรวมข้อมูลดิบก่อนการสกัด")

# --- Main Content ---

if not api_key:
    st.warning("โปรดใส่ **Gemini API Key** ใน Sidebar เพื่อเริ่มต้นใช้งาน")
else:
    try:
        client = genai.Client(api_key=api_key)
        
        # 1. Topic Input
        st.header("1. ระบุเหตุการณ์ภัยพิบัติที่ต้องการสรุป")
        disaster_topic = st.text_input(
            "ตัวอย่าง: 'แผ่นดินไหวตุรกี กุมภาพันธ์ 2023'",
            value="น้ำท่วมหาดใหญ่ 2024",
            help="กรอกชื่อเหตุการณ์และช่วงเวลาให้เจาะจง"
        )
        
        # 2. Processing Button
        if st.button("🚀 รวบรวมข้อมูล, สกัด และวิเคราะห์", type="primary"):
            if disaster_topic:
                
                # --- ขั้นตอนที่ 1: รวบรวมข้อมูลดิบ ---
                raw_news_texts = get_placeholder_data(client, disaster_topic) 
                    
                if "ไม่พบข้อมูล" in raw_news_texts:
                    st.stop()
                
                # --- ขั้นตอนที่ 2: สกัดข้อมูลด้วย LLM และ JSON Schema ---
                with st.spinner("⏳ ขั้นตอนที่ 2: กำลังวิเคราะห์ชุดข้อความและสร้าง JSON DataFrame ด้วย Gemini API..."):
                    
                    system_prompt, user_prompt = create_summary_prompt(disaster_topic)
                    
                    # รวมข้อมูลดิบทั้งหมดเข้ากับ User Prompt เพื่อให้ LLM ประมวลผล
                    full_user_prompt = user_prompt + "\n\nชุดข้อความข่าว:\n" + raw_news_texts
                    
                    # Call Gemini API พร้อม System Prompt และ JSON Schema
                    response = client.models.generate_content(
                        model='gemini-2.5-flash',
                        contents=[full_user_prompt],
                        config=types.GenerateContentConfig(
                            system_instruction=system_prompt,
                            response_mime_type="application/json", # บังคับ Output เป็น JSON
                            response_schema=types.Schema(**JSON_SCHEMA), # ใช้ Schema ที่กำหนด
                            temperature=0.4 # เพิ่มอุณหภูมิเล็กน้อยเพื่อให้ LLM สรุปเนื้อหาได้แตกต่าง
                        )
                    )
                    
                    json_output = response.text.strip()

                if json_output:
                    
                    # --- ขั้นตอนที่ 3: แปลง JSON เป็น Pandas DataFrame และแสดงผล ---
                    st.header("2. ตารางสรุปข้อมูลภัยพิบัติ (Pandas DataFrame)")
                    
                    try:
                        data = json.loads(json_output)
                        df = pd.DataFrame(data)
                        
                        # ทำความสะอาดข้อมูลวันที่
                        df['วันที่_และ_เวลา'] = pd.to_datetime(df['วันที่_และ_เวลา'], errors='coerce')
                        
                        # กรองแถวที่วันที่ไม่ถูกต้อง (NaN) ที่ LLM สกัดไม่ได้ออก
                        df.dropna(subset=['วันที่_และ_เวลา'], inplace=True)
                        
                        df = df.sort_values(by='วันที่_และ_เวลา').reset_index(drop=True)
                        
                        st.success(f"✅ สกัดและจัดโครงสร้างข้อมูลสำเร็จ! (จำนวนข่าวที่ใช้ได้: {len(df)} รายการ)")
                        st.dataframe(df, use_container_width=True)
                        
                        # Download Feature
                        csv_download = df.to_csv(index=False).encode('utf-8')
                        st.download_button(
                            label="⬇️ ดาวน์โหลดผลลัพธ์เป็น CSV",
                            data=csv_download,
                            file_name=f'disaster_summary_{disaster_topic.replace(" ", "_")}.csv',
                            mime='text/csv',
                        )

                        # --- ขั้นตอนที่ 4: สร้างกราฟวิเคราะห์ (Plotly) ---
                        st.header("3. กราฟวิเคราะห์ตามช่วงเวลา")
                        
                        # คำนวณผลรวมสะสม (Cumulative Sum)
                        df['ผู้เสียชีวิต_สะสม'] = df['ผู้เสียชีวิต_จำนวน'].cumsum()
                        df['ผู้บาดเจ็บ_สะสม'] = df['ผู้บาดเจ็บ_จำนวน'].cumsum()

                        # กราฟ 1: ผู้เสียชีวิตสะสม
                        fig1 = px.line(
                            df, 
                            x='วันที่_และ_เวลา', 
                            y='ผู้เสียชีวิต_สะสม', 
                            title='📈 จำนวนผู้เสียชีวิตสะสมตามช่วงเวลา',
                            labels={'ผู้เสียชีวิต_สะสม': 'จำนวนผู้เสียชีวิตสะสม', 'วันที่_และ_เวลา': 'วันที่และเวลาที่รายงาน'}
                        )
                        st.plotly_chart(fig1, use_container_width=True)
                        
                        # กราฟ 2: ผู้บาดเจ็บสะสม
                        fig2 = px.line(
                            df, 
                            x='วันที่_และ_เวลา', 
                            y='ผู้บาดเจ็บ_สะสม', 
                            title='📈 จำนวนผู้บาดเจ็บสะสมตามช่วงเวลา',
                            labels={'ผู้บาดเจ็บ_สะสม': 'จำนวนผู้บาดเจ็บสะสม', 'วันที่_และ_เวลา': 'วันที่และเวลาที่รายงาน'}
                        )
                        st.plotly_chart(fig2, use_container_width=True)
                        
                    except json.JSONDecodeError:
                        st.error("❌ LLM ไม่สามารถสร้าง JSON ที่ถูกต้องตาม Schema ได้")
                        st.subheader("Raw JSON Output (สำหรับตรวจสอบ)")
                        st.code(json_output)
                    except Exception as e:
                        st.error(f"เกิดข้อผิดพลาดในการประมวลผลข้อมูลหรือสร้างกราฟ: {e}")
                else:
                    st.warning("ไม่สามารถสร้าง Output จาก Gemini API ได้")
            else:
                st.warning("โปรดระบุหัวข้อเหตุการณ์ภัยพิบัติ")
                
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดในการเริ่มต้น Client: {e}")