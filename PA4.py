import streamlit as st
import pandas as pd
import io
import json # ยังคงใช้ json สำหรับการโหลดข้อมูลดิบเท่านั้น
import plotly.express as px
from google import genai
from google.genai import types
from typing import List, Dict, Any, Tuple

# --- 1. CONFIGURATION AND UTILITIES ---

# หัวข้อตารางที่เราต้องการให้ LLM สร้าง (ใช้ในการกำหนดรูปแบบ CSV)
CSV_HEADERS = "id_ข่าว,วันที่_และ_เวลา,แหล่งที่มา_ของ_ข่าว,เนื้อหา_ข่าว_สรุป,ผู้เสียชีวิต_จำนวน,ผู้บาดเจ็บ_จำนวน"

def create_summary_prompt(disaster_topic: str) -> Tuple[str, str]:
    """สร้าง System และ User Prompt สำหรับการสกัดข้อมูลเชิงปริมาณในรูปแบบ CSV"""
    
    system_prompt = (
        "คุณคือผู้เชี่ยวชาญด้านการวิเคราะห์ข้อมูลภัยพิบัติ หน้าที่ของคุณคือการวิเคราะห์ชุดข้อความข่าวที่แตกต่างกัน "
        "ที่เกี่ยวข้องกับเหตุการณ์ '"+ disaster_topic +"' และสกัดข้อมูลออกมาเป็นตารางในรูปแบบ CSV "
        "โดยมีหัวข้อดังนี้: " + CSV_HEADERS + "\n\n"
        "กฎหลัก: 1. ต้องสกัด 'วันที่_และ_เวลา' ออกจากเนื้อหาข่าวด้วยความแม่นยำ 2. 'ผู้เสียชีวิต_จำนวน' และ 'ผู้บาดเจ็บ_จำนวน' ต้องเป็นตัวเลขจำนวนเต็ม "
        "เงื่อนไขสำคัญ: **หากข่าวระบุว่าไม่มีผู้เสียชีวิตหรือผู้บาดเจ็บ ให้ใส่ค่าเป็น 0 (ศูนย์) ห้ามใช้ค่าว่างหรือ Null** 3. 'เนื้อหา_ข่าว_สรุป' ต้องเป็นพารากราฟที่แตกต่างกันในแต่ละข่าว มีความยาว 100-300 คำ\n"
        "**กฎการกรองที่เข้มงวด: หากข่าวใดไม่สามารถสกัดข้อมูลที่จำเป็น (วันที่/เวลา, จำนวนผู้เสียชีวิต, หรือจำนวนผู้บาดเจ็บ) ได้อย่างชัดเจน คุณต้องละทิ้งข่าวรายการนั้นทันที** "
        "ผลลัพธ์สุดท้ายต้องเป็นข้อมูล CSV เท่านั้น โดยขึ้นต้นด้วยแถวหัวข้อ (Header) และไม่มีคำอธิบายอื่น ๆ ก่อนหรือหลัง CSV"
    )
    
    user_prompt = f"""
    โปรดวิเคราะห์ชุดข้อความข่าวจำนวนมากที่ฉันจะนำเสนอให้คุณ ซึ่งเกี่ยวข้องกับเหตุการณ์ '{disaster_topic}' 
    และสร้างตาราง CSV ตามกฎข้างต้น
    """
    
    return system_prompt, user_prompt

def get_placeholder_data(client: genai.Client, topic: str) -> str:
    """
    ใช้ Gemini API พร้อม Google Search Tool เพื่อค้นหาข้อมูลดิบ 
    """
    
    st.info(f"🔍 ขั้นตอนที่ 1: กำลังค้นหาข้อมูลจากเว็บเกี่ยวกับ '{topic}' โดยใช้ Google Search Tool...")
    
    try:
        # Prompt สั่งให้ Gemini ค้นหาข่าวที่มีตัวเลขและวันที่ชัดเจน (เพื่อเพิ่มโอกาสได้ข้อมูลครบถ้วน)
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=[f"ค้นหาข่าวสารและรายงานล่าสุดอย่างน้อย 20 แหล่งเกี่ยวกับเหตุการณ์ {topic} ที่มีข้อมูล **จำนวนผู้เสียชีวิต** และ **ผู้บาดเจ็บ** พร้อมวันที่และแหล่งที่มาที่แตกต่างกัน"],
            config=types.GenerateContentConfig(
                tools=[{"google_search": {}}]
            )
        )
        
        if response.candidates and response.candidates[0].grounding_metadata and response.candidates[0].grounding_metadata.web_search_queries:
            
            search_queries = response.candidates[0].grounding_metadata.web_search_queries
            
            all_texts = []
            
            for i, query in enumerate(search_queries):
                source_name = f"Search Source {i+1} (Query: {query[:20]}...)"
                all_texts.append(f"--- NEWS ITEM ---\nID: {i+1}\nแหล่งที่มา: {source_name}\nเนื้อหาข่าวสาร: {query}")
                
            st.success(f"✅ รวบรวมข้อมูลดิบสำหรับการสกัดสำเร็จ (จำนวนแหล่งข่าวเริ่มต้น: {len(all_texts)} รายการ)")
            return "\n".join(all_texts)
        
        return "ไม่พบข้อมูลข่าวสารที่เกี่ยวข้อง"

    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดขณะเรียกใช้ Gemini Tool: {e}")
        return "ไม่พบข้อมูลข่าวสารที่เกี่ยวข้อง"

# --- 2. STREAMLIT APP LAYOUT ---

st.set_page_config(
    page_title="Disaster Event Summarizer (DES) 📊",
    layout="wide"
)

st.title("🌊 Disaster Event Summarizer (DES) and Analysis")
st.caption("รวบรวมข้อมูลภัยพิบัติจากหลายแหล่ง สกัดตัวเลข และสร้างกราฟวิเคราะห์ตามเวลา")

# --- Sidebar for API Key ---

with st.sidebar:
    st.header("🔑 Gemini API Configuration")
    
    api_key = st.text_input(
        "Enter your Gemini API Key", 
        type="password", 
        help="หาได้จาก Google AI Studio"
    )
    st.divider()
    st.info("💡 หมายเหตุ: แอปนี้ใช้ Google Search Tool ในการรวบรวมข้อมูลดิบก่อนการสกัด")

# --- Main Content ---

if not api_key:
    st.warning("โปรดใส่ **Gemini API Key** ใน Sidebar เพื่อเริ่มต้นใช้งาน")
else:
    try:
        client = genai.Client(api_key=api_key)
        
        # 1. Topic Input
        st.header("1. ระบุเหตุการณ์ภัยพิบัติที่ต้องการสรุป")
        disaster_topic = st.text_input(
            "ตัวอย่าง: 'แผ่นดินไหวตุรกี กุมภาพันธ์ 2023'",
            value="แผ่นดินไหวที่ไต้หวัน เมษายน 2024",
            help="กรอกชื่อเหตุการณ์และช่วงเวลาให้เจาะจง"
        )
        
        # 2. Processing Button
        if st.button("🚀 รวบรวมข้อมูล, สกัด และวิเคราะห์", type="primary"):
            if disaster_topic:
                
                # --- ขั้นตอนที่ 1: รวบรวมข้อมูลดิบ ---
                raw_news_texts = get_placeholder_data(client, disaster_topic) 
                    
                if "ไม่พบข้อมูล" in raw_news_texts:
                    st.stop()
                
                # --- ขั้นตอนที่ 2: สกัดข้อมูลด้วย LLM และ CSV Formatting ---
                with st.spinner("⏳ ขั้นตอนที่ 2: กำลังวิเคราะห์ชุดข้อความและสร้าง CSV DataFrame ด้วย Gemini API..."):
                    
                    system_prompt, user_prompt = create_summary_prompt(disaster_topic)
                    
                    full_user_prompt = user_prompt + "\n\nชุดข้อความข่าว:\n" + raw_news_texts
                    
                    # Call Gemini API: ไม่ใช้ response_mime_type หรือ response_schema
                    response = client.models.generate_content(
                        model='gemini-2.5-flash',
                        contents=[full_user_prompt],
                        config=types.GenerateContentConfig(
                            system_instruction=system_prompt,
                            temperature=0.4 
                        )
                    )
                    
                    csv_output = response.text.strip()

                if csv_output:
                    
                    # --- ขั้นตอนที่ 3: แปลง CSV เป็น Pandas DataFrame และแสดงผล ---
                    st.header("2. ตารางสรุปข้อมูลภัยพิบัติ (Pandas DataFrame)")
                    
                    try:
                        # ใช้ StringIO เพื่อแปลง String CSV เป็นไฟล์ที่ Pandas อ่านได้
                        csv_data = io.StringIO(csv_output)
                        
                        # อ่าน CSV โดยคาดหวังว่า LLM จะสร้างคอลัมน์และข้อมูลถูกต้อง
                        df = pd.read_csv(csv_data)
                        
                        # ตรวจสอบว่ามีข้อมูลอื่นที่ไม่ใช่ตารางติดมาหรือไม่
                        if len(df.columns) != len(CSV_HEADERS.split(',')) or len(df) == 0:
                            raise ValueError("รูปแบบ CSV ไม่ถูกต้องหรือไม่สมบูรณ์")

                        # ทำความสะอาดข้อมูลวันที่
                        df['วันที่_และ_เวลา'] = pd.to_datetime(df['วันที่_และ_เวลา'], errors='coerce')
                        
                        # กรองแถวที่วันที่ไม่ถูกต้อง (NaN) ออก
                        df.dropna(subset=['วันที่_และ_เวลา'], inplace=True)
                        
                        # ตรวจสอบอีกครั้งว่า DataFrame ว่างหรือไม่หลังจากกรอง
                        if df.empty:
                            st.error("❌ ข้อมูลถูกสกัดสำเร็จ แต่หลังจากกรองแถวที่ไม่สมบูรณ์ (ไม่มีวันที่/เวลาที่ถูกต้อง) ทำให้ตารางว่างเปล่า")
                            st.warning("โปรดตรวจสอบว่าหัวข้อที่ค้นหามีข้อมูลวันที่และเวลาที่ชัดเจน")
                            st.subheader("Raw CSV Output (สำหรับตรวจสอบ)")
                            st.code(csv_output)
                            st.stop()

                        df = df.sort_values(by='วันที่_และ_เวลา').reset_index(drop=True)
                        
                        st.success(f"✅ สกัดและจัดโครงสร้างข้อมูลสำเร็จ! (จำนวนข่าวที่ใช้ได้: {len(df)} รายการ)")
                        st.dataframe(df, use_container_width=True)
                        
                        # Download Feature
                        csv_download = df.to_csv(index=False).encode('utf-8')
                        st.download_button(
                            label="⬇️ ดาวน์โหลดผลลัพธ์เป็น CSV",
                            data=csv_download,
                            file_name=f'disaster_summary_{disaster_topic.replace(" ", "_")}.csv',
                            mime='text/csv',
                        )

                        # --- ขั้นตอนที่ 4: สร้างกราฟวิเคราะห์ (Plotly) ---
                        st.header("3. กราฟวิเคราะห์ตามช่วงเวลา")
                        
                        # แปลงคอลัมน์ตัวเลขให้แน่ใจว่าเป็นตัวเลข (เผื่อ LLM ใส่ String ที่ไม่ใช่ตัวเลขเข้ามา)
                        df['ผู้เสียชีวิต_จำนวน'] = pd.to_numeric(df['ผู้เสียชีวิต_จำนวน'], errors='coerce').fillna(0).astype(int)
                        df['ผู้บาดเจ็บ_จำนวน'] = pd.to_numeric(df['ผู้บาดเจ็บ_จำนวน'], errors='coerce').fillna(0).astype(int)
                        
                        # คำนวณผลรวมสะสม (Cumulative Sum)
                        df['ผู้เสียชีวิต_สะสม'] = df['ผู้เสียชีวิต_จำนวน'].cumsum()
                        df['ผู้บาดเจ็บ_สะสม'] = df['ผู้บาดเจ็บ_จำนวน'].cumsum()

                        # กราฟ 1: ผู้เสียชีวิตสะสม
                        fig1 = px.line(
                            df, 
                            x='วันที่_และ_เวลา', 
                            y='ผู้เสียชีวิต_สะสม', 
                            title='📈 จำนวนผู้เสียชีวิตสะสมตามช่วงเวลา',
                            labels={'ผู้เสียชีวิต_สะสม': 'จำนวนผู้เสียชีวิตสะสม', 'วันที่_และ_เวลา': 'วันที่และเวลาที่รายงาน'}
                        )
                        st.plotly_chart(fig1, use_container_width=True)
                        
                        # กราฟ 2: ผู้บาดเจ็บสะสม
                        fig2 = px.line(
                            df, 
                            x='วันที่_และ_เวลา', 
                            y='ผู้บาดเจ็บ_สะสม', 
                            title='📈 จำนวนผู้บาดเจ็บสะสมตามช่วงเวลา',
                            labels={'ผู้บาดเจ็บ_สะสม': 'จำนวนผู้บาดเจ็บสะสม', 'วันที่_และ_เวลา': 'วันที่และเวลาที่รายงาน'}
                        )
                        st.plotly_chart(fig2, use_container_width=True)
                        
                    except ValueError as ve:
                        st.error(f"❌ เกิดข้อผิดพลาดในการประมวลผล CSV: {ve}")
                        st.warning("สาเหตุ: LLM อาจสร้างข้อมูลที่ไม่ใช่ CSV, ขาดหัวข้อ หรือมีเครื่องหมายจุลภาคผิดตำแหน่ง")
                        st.subheader("Raw CSV Output (สำหรับตรวจสอบ)")
                        st.code(csv_output)
                    except KeyError as ke:
                        st.error(f"❌ เกิดข้อผิดพลาด: คอลัมน์ '{ke.args[0]}' หายไป")
                        st.warning("สาเหตุ: LLM ไม่ได้สร้างหัวข้อ CSV ตามที่กำหนด")
                        st.subheader("Raw CSV Output (สำหรับตรวจสอบ)")
                        st.code(csv_output)
                    except Exception as e:
                        st.error(f"เกิดข้อผิดพลาดในการประมวลผลข้อมูลหรือสร้างกราฟ: {e}")
                else:
                    st.warning("ไม่สามารถสร้าง Output จาก Gemini API ได้")
            else:
                st.warning("โปรดระบุหัวข้อเหตุการณ์ภัยพิบัติ")
                
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดในการเริ่มต้น Client: {e}")