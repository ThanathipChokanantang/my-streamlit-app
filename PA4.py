import streamlit as st
import pandas as pd
import io
import json
import plotly.express as px
from google import genai
from google.genai import types
from typing import List, Dict, Any, Tuple

# --- 1. CONFIGURATION AND UTILITIES ---

# JSON Format Description ถูกแปลงเป็นข้อความเพื่อใช้ใน Prompt (อัปเดตแล้ว)
JSON_FORMAT_DESCRIPTION = """
[
  {
    "ลำดับ_เหตุการณ์": (integer),
    "วันที่_หรือ_ช่วงเวลา": "(เดือน/ปี หรือปีเท่านั้น, เช่น YYYY-MM หรือ YYYY)",
    "มูลค่า_ความเสียหาย_บาท": (float หรือ number),
    "ผู้เสียชีวิต_จำนวน": (integer),
    "ผู้บาดเจ็บ_จำนวน": (integer),
    "แหล่งที่มา_ของ_ข่าว": "(ชื่อเว็ปไซต์, สำนักข่าว, และลิงก์อ้างอิงหลายแหล่งก็ได้ โดยคั่นด้วยเครื่องหมายจุลภาค)",
    "รายละเอียด_ของ_เหตุการณ์": "(ข้อความสรุปเหตุการณ์ 100-300 คำ อธิบายสาเหตุ พื้นที่ และผลกระทบ)"
  }
  // ... รายการเหตุการณ์อื่นๆ
]
"""

def create_raw_search_prompt(event_type: str, location: str) -> str:
    """Prompt สำหรับขั้นตอนที่ 1: ค้นหาและสรุปข้อมูลดิบ (Tool Use)"""
    
    return (
        f"ค้นหาข้อมูลสถิติที่เกี่ยวข้องกับเหตุการณ์ภัยพิบัติประเภท '{event_type}' ที่เคยเกิดขึ้นในภูมิภาค '{location}' ทั้งหมดในอดีต "
        "โดยเน้นไปที่การรายงานวันที่/ช่วงเวลา, มูลค่าความเสียหาย, จำนวนผู้เสียชีวิต, ผู้บาดเจ็บ, **แหล่งที่มาของข่าว (เว็บไซต์/ลิงก์)** และ **รายละเอียดสรุปเหตุการณ์** "
        "ให้สรุปข้อมูลที่พบทั้งหมดออกมาเป็น **ข้อความยาว ๆ** ที่มีรายละเอียดเพียงพอสำหรับการสกัดข้อมูลสถิติในภายหลัง"
    )

def create_extraction_prompt(event_type: str, location: str) -> str:
    """Prompt สำหรับขั้นตอนที่ 2: สกัดข้อมูลจากข้อความดิบที่ให้มา (บังคับ JSON ด้วย Prompt)"""
    
    # System Prompt ที่เน้นการสกัดตัวเลข, รายละเอียด และแหล่งที่มา
    system_prompt = (
        "คุณคือผู้เชี่ยวชาญด้านการวิเคราะห์ข้อมูลทางประวัติศาสตร์ หน้าที่ของคุณคือการวิเคราะห์ 'ข้อความดิบ' ที่ฉันจะให้คุณ "
        "และสกัดข้อมูลสถิติของเหตุการณ์ภัยพิบัติ '" + event_type + "' ใน '" + location + "' ออกมาในรูปแบบ **JSON Array ที่ถูกต้อง 100%** "
        "กฎที่เข้มงวด: "
        f"1. รูปแบบ JSON Array ต้องเป็นไปตามโครงสร้างนี้เท่านั้น:\n{JSON_FORMAT_DESCRIPTION}\n"
        "2. คอลัมน์ 'รายละเอียด_ของ_เหตุการณ์' ต้องมีเนื้อหาที่แตกต่างกันทุกเหตุการณ์ และความยาว 100-300 คำ เพื่ออธิบายบริบทของเหตุการณ์นั้น\n"
        "3. **หากไม่พบตัวเลขความเสียหาย, ผู้เสียชีวิต, หรือผู้บาดเจ็บอย่างชัดเจน ให้ใส่ค่าเป็น 0 (ศูนย์) ห้ามใช้ค่า Null หรือละเว้นคีย์** "
        "4. **ห้ามมีข้อความใด ๆ** ก่อนหรือหลัง JSON Array โดยเด็ดขาด"
    )
    return system_prompt

def get_raw_summary(client: genai.Client, event_type: str, location: str) -> str:
    """ขั้นตอนที่ 1: ใช้ Tool Search เพื่อค้นหาและสรุปข้อมูลดิบ (Text Output)"""
    
    prompt = create_raw_search_prompt(event_type, location)
    
    try:
        response = client.models.generate_content(
            model='gemini-2.5-flash',
            contents=[prompt],
            config=types.GenerateContentConfig(
                tools=[{"google_search": {}}]
            )
        )
        return response.text
        
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดในการค้นหาข้อมูล (ขั้นตอนที่ 1): {e}")
        return None

# --- 2. STREAMLIT APP LAYOUT ---

st.set_page_config(
    page_title="Disaster Event Statistics Analyzer 📊",
    layout="wide"
)

st.title("📊 Disaster Event Statistics Analyzer")
st.caption("วิเคราะห์สถิติความเสียหาย, ผู้เสียชีวิต, และผู้บาดเจ็บของภัยพิบัติในอดีต")


# 🔑 Gemini API Configuration

with st.sidebar:
    st.header("🔑 Gemini API Configuration")
    api_key = st.text_input("Enter your Gemini API Key", type="password")
    st.divider()
    st.info("💡 โปรแกรมนี้ใช้ Google Search Tool (ขั้นตอนที่ 1) และการสกัด JSON (ขั้นตอนที่ 2) แยกกัน")


# 🔎 Main Content

if not api_key:
    st.warning("โปรดใส่ **Gemini API Key** ใน Sidebar เพื่อเริ่มต้นใช้งาน")
else:
    try:
        client = genai.Client(api_key=api_key)
        
        # 1.1 & 1.2: Input Event & Location
        st.header("1. ระบุเหตุการณ์และสถานที่")
        
        col1, col2 = st.columns(2)
        
        event_type = col1.text_input(
            "1.1 ประเภทภัยพิบัติ (เช่น น้ำท่วม, แผ่นดินไหว)",
            value="แผ่นดินไหว",
        )
        location = col2.text_input(
            "1.2 สถานที่ (เช่น กรุงเทพ, ญี่ปุ่น, ทั้งโลก)",
            value="ญี่ปุ่น",
        )
        
        # 2. Processing Button
        if st.button("🚀 สรุปสถิติและวิเคราะห์", type="primary"): 
            if event_type and location:
                
                # --- ขั้นตอนที่ 1: ค้นหาข้อมูลดิบ (Tool Use) ---
                with st.spinner(f"⏳ ขั้นตอนที่ 1: กำลังค้นหาข้อมูลดิบเกี่ยวกับ '{event_type}' ใน '{location}'..."):
                    raw_text_summary = get_raw_summary(client, event_type, location)
                
                if not raw_text_summary:
                    st.stop()
                
                st.info("✅ ค้นพบข้อมูลดิบแล้ว! กำลังเข้าสู่ขั้นตอนการสกัดข้อมูลเชิงโครงสร้าง...")

                # --- ขั้นตอนที่ 2: สกัดข้อมูล (Prompt Formatting) ---
                with st.spinner("⏳ ขั้นตอนที่ 2: กำลังวิเคราะห์และสกัดข้อมูลเป็น JSON Array..."):
                    
                    system_prompt_extract = create_extraction_prompt(event_type, location)
                    user_prompt_extract = "ข้อความดิบที่ต้องวิเคราะห์:\n\n" + raw_text_summary

                    response_extract = client.models.generate_content(
                        model='gemini-2.5-flash',
                        contents=[user_prompt_extract],
                        config=types.GenerateContentConfig(
                            system_instruction=system_prompt_extract,
                        )
                    )
                    
                    json_output = response_extract.text.strip()
                    
                    # ทำความสะอาด JSON output ที่อาจมี ```json และ \n ติดมา
                    if json_output.startswith('```'):
                        json_output = json_output.strip().replace('```json', '').replace('```', '')

                if json_output:
                    
                    # --- ขั้นตอนที่ 3: แปลง JSON เป็น Pandas DataFrame และแสดงผล ---
                    st.header("2. ตารางสรุปสถิติเหตุการณ์")
                    
                    try:
                        data: List[Dict[str, Any]] = json.loads(json_output)
                        
                        if not data:
                            st.error(f"❌ ไม่พบข้อมูลสถิติเหตุการณ์ '{event_type}' ใน '{location}' ที่มีข้อมูลครบถ้วน")
                            st.warning("โปรดลองระบุสถานที่หรือประเภทภัยพิบัติที่กว้างขึ้น")
                            st.subheader("Raw JSON Output (สำหรับตรวจสอบ)")
                            st.code(json_output)
                            st.stop()
                            
                        df = pd.DataFrame(data)
                        
                        # ทำความสะอาดข้อมูลวันที่และเรียงลำดับ
                        df['วันที่_หรือ_ช่วงเวลา_Sort'] = pd.to_datetime(df['วันที่_หรือ_ช่วงเวลา'], errors='coerce')
                        df.dropna(subset=['วันที่_หรือ_ช่วงเวลา_Sort'], inplace=True) 
                        df = df.sort_values(by='วันที่_หรือ_ช่วงเวลา_Sort').reset_index(drop=True)
                        df.drop(columns=['วันที่_หรือ_ช่วงเวลา_Sort'], inplace=True)
                        
                        st.success(f"✅ สกัดและจัดโครงสร้างข้อมูลสำเร็จ! (จำนวนเหตุการณ์ที่สกัดได้: {len(df)} ครั้ง)")
                        
                        # กำหนดคอลัมน์ที่จะแสดงใน DataFrame (รวมคอลัมน์ใหม่)
                        display_cols = [
                            'ลำดับ_เหตุการณ์',
                            'วันที่_หรือ_ช่วงเวลา',
                            'มูลค่า_ความเสียหาย_บาท',
                            'ผู้เสียชีวิต_จำนวน',
                            'ผู้บาดเจ็บ_จำนวน',
                            'แหล่งที่มา_ของ_ข่าว',
                            'รายละเอียด_ของ_เหตุการณ์'
                        ]
                        
                        # กรองให้เหลือเฉพาะคอลัมน์ที่ต้องการแสดงผล
                        df_display = df.reindex(columns=display_cols, fill_value='')

                        st.dataframe(df_display, use_container_width=True)
                        
                        # --- ขั้นตอนที่ 4: สร้าง Bar Chart วิเคราะห์ (ใช้คอลัมน์เดิม) ---
                        st.header("3. กราฟวิเคราะห์สถิติ (Bar Charts)")
                        
                        df['เหตุการณ์_Label'] = df['วันที่_หรือ_ช่วงเวลา'] + ' (' + df['ลำดับ_เหตุการณ์'].astype(str) + ')'
                        
                        # 4.1 กราฟมูลค่าความเสียหาย
                        fig1 = px.bar(
                            df, 
                            x='เหตุการณ์_Label', 
                            y='มูลค่า_ความเสียหาย_บาท', 
                            title='📈 มูลค่าความเสียหายรวม (บาท) ในแต่ละเหตุการณ์',
                            labels={'มูลค่า_ความเสียหาย_บาท': 'มูลค่าความเสียหาย (บาท)', 'เหตุการณ์_Label': 'วันที่/ช่วงเวลาเหตุการณ์'},
                            height=400,
                            color='มูลค่า_ความเสียหาย_บาท',
                            color_continuous_scale=px.colors.sequential.Sunset
                        )
                        st.plotly_chart(fig1, use_container_width=True)
                        
                        # 4.2 กราฟจำนวนผู้เสียชีวิต
                        fig2 = px.bar(
                            df, 
                            x='เหตุการณ์_Label', 
                            y='ผู้เสียชีวิต_จำนวน', 
                            title='💔 จำนวนผู้เสียชีวิต (คน) ในแต่ละเหตุการณ์',
                            labels={'ผู้เสียชีวิต_จำนวน': 'จำนวนผู้เสียชีวิต (คน)', 'เหตุการณ์_Label': 'วันที่/ช่วงเวลาเหตุการณ์'},
                            height=400,
                            color='ผู้เสียชีวิต_จำนวน',
                            color_continuous_scale=px.colors.sequential.Reds
                        )
                        st.plotly_chart(fig2, use_container_width=True)
                        
                        # 4.3 กราฟจำนวนผู้บาดเจ็บ
                        fig3 = px.bar(
                            df, 
                            x='เหตุการณ์_Label', 
                            y='ผู้บาดเจ็บ_จำนวน', 
                            title='🩹 จำนวนผู้ได้รับบาดเจ็บ (คน) ในแต่ละเหตุการณ์',
                            labels={'ผู้บาดเจ็บ_จำนวน': 'จำนวนผู้บาดเจ็บ (คน)', 'เหตุการณ์_Label': 'วันที่/ช่วงเวลาเหตุการณ์'},
                            height=400,
                            color='ผู้บาดเจ็บ_จำนวน',
                            color_continuous_scale=px.colors.sequential.Blues
                        )
                        st.plotly_chart(fig3, use_container_width=True)
                        
                    except json.JSONDecodeError:
                        st.error("❌ เกิดข้อผิดพลาดในการประมวลผล JSON")
                        st.warning("สาเหตุ: LLM ไม่สามารถสร้าง JSON ที่ถูกต้องตามโครงสร้างได้")
                        st.subheader("Raw JSON Output (สำหรับตรวจสอบ)")
                        st.code(json_output)
                    except Exception as e:
                        st.error(f"เกิดข้อผิดพลาดในการประมวลผลข้อมูลหรือสร้างกราฟ: {e}")
                else:
                    st.warning("ไม่สามารถสร้าง Output จาก Gemini API ได้")
            else:
                st.warning("โปรดระบุประเภทภัยพิบัติและสถานที่")
                
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดในการเริ่มต้น Client: {e}")