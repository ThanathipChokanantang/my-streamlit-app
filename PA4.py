import streamlit as st
import pandas as pd
import io
import json
import plotly.express as px
from google import genai
from google.genai import types
from typing import List, Dict, Any, Tuple

# --- 1. CONFIGURATION AND UTILITIES ---

# JSON Schema ใหม่ เน้นการสกัดข้อมูลเชิงปริมาณและวันที่
JSON_SCHEMA = {
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "ลำดับ_เหตุการณ์": {"type": "integer", "description": "ลำดับเหตุการณ์ในอดีต (1, 2, 3...)"},
            # ใช้ String เพื่อให้ยืดหยุ่นในการสกัด เดือน/ปี/ปี+เดือน
            "วันที่_หรือ_ช่วงเวลา": {"type": "string", "description": "เดือน/ปี/ช่วงเวลาที่เกิดเหตุการณ์นั้น (เช่น YYYY-MM หรือ YYYY)"}, 
            # มูลค่าความเสียหาย (ตัวเลข Float)
            "มูลค่า_ความเสียหาย_บาท": {"type": "number", "description": "มูลค่าความเสียหายรวมที่สกัดได้จากข่าว (ตัวเลข float เท่านั้น หากไม่ระบุให้ใส่ 0)"},
            # ผู้เสียชีวิต (ตัวเลข Integer)
            "ผู้เสียชีวิต_จำนวน": {"type": "integer", "description": "จำนวนผู้เสียชีวิต (จำนวนเต็มเท่านั้น หากไม่ระบุให้ใส่ 0)"},
            # ผู้บาดเจ็บ (ตัวเลข Integer)
            "ผู้บาดเจ็บ_จำนวน": {"type": "integer", "description": "จำนวนผู้บาดเจ็บ (จำนวนเต็มเท่านั้น หากไม่ระบุให้ใส่ 0)"}
        },
        # บังคับว่าทุกฟิลด์ต้องมีค่า (ต้องเป็นตัวเลข/string ที่ถูกต้อง ไม่ใช่ null)
        "required": ["ลำดับ_เหตุการณ์", "วันที่_หรือ_ช่วงเวลา", "มูลค่า_ความเสียหาย_บาท", "ผู้เสียชีวิต_จำนวน", "ผู้บาดเจ็บ_จำนวน"]
    }
}

def create_summary_prompt(event_type: str, location: str) -> Tuple[str, str]:
    """สร้าง System และ User Prompt สำหรับการสกัดข้อมูลสถิติ"""
    
    # System Prompt ที่เน้นการสกัดตัวเลข และใช้ 0 แทนค่า Null
    system_prompt = (
        "คุณคือผู้เชี่ยวชาญด้านการวิเคราะห์ข้อมูลทางประวัติศาสตร์ หน้าที่ของคุณคือการค้นหาและรวบรวมข้อมูลสถิติของ "
        "เหตุการณ์ภัยพิบัติประเภท '" + event_type + "' ที่เคยเกิดขึ้นใน '" + location + "' ในอดีต "
        "และสร้างผลลัพธ์ในรูปแบบ JSON Array ตาม Schema ที่กำหนดอย่างเคร่งครัด "
        "กฎที่เข้มงวด: "
        "1. คุณต้องระบุ **ทุกเหตุการณ์** ที่เคยเกิดขึ้นในอดีต (ไม่จำกัดจำนวน) "
        "2. ต้องสกัด 'วันที่_หรือ_ช่วงเวลา' (เดือน/ปี หรือปีเท่านั้น) 'มูลค่า_ความเสียหาย_บาท', 'ผู้เสียชีวิต_จำนวน', และ 'ผู้บาดเจ็บ_จำนวน' สำหรับแต่ละเหตุการณ์ "
        "3. **หากไม่พบตัวเลขความเสียหาย, ผู้เสียชีวิต, หรือผู้บาดเจ็บอย่างชัดเจน ให้ใส่ค่าเป็น 0 (ศูนย์) ห้ามใช้ค่า Null หรือละเว้นคีย์** "
        "4. **ห้ามมีคำอธิบายอื่นใด** ก่อนหรือหลัง JSON Array ผลลัพธ์จะต้องเป็น JSON Array เท่านั้น"
    )
    
    user_prompt = f"""
    โปรดค้นหาและสรุปสถิติของเหตุการณ์ '{event_type}' ที่เคยเกิดขึ้นในภูมิภาค '{location}' ทั้งหมดในอดีต 
    และแปลงข้อมูลนั้นให้อยู่ในโครงสร้าง JSON Array โดยมีมูลค่าความเสียหายในหน่วยบาท (Thai Baht) 
    ใช้ Google Search Tool เพื่อรวบรวมข้อมูลสถิติที่เกี่ยวข้อง
    """
    
    return system_prompt, user_prompt

# --- 2. STREAMLIT APP LAYOUT ---

st.set_page_config(
    page_title="Disaster Event Statistics Analyzer 📊",
    layout="wide"
)

st.title("📊 Disaster Event Statistics Analyzer")
st.caption("วิเคราะห์สถิติความเสียหาย, ผู้เสียชีวิต, และผู้บาดเจ็บของภัยพิบัติในอดีต")

# --- Sidebar for API Key ---

with st.sidebar:
    st.header("🔑 Gemini API Configuration")
    api_key = st.text_input("Enter your Gemini API Key", type="password")
    st.divider()
    st.info("💡 โปรแกรมนี้ใช้ Google Search Tool ในการค้นหาสถิติ")

# --- Main Content ---

if not api_key:
    st.warning("โปรดใส่ **Gemini API Key** ใน Sidebar เพื่อเริ่มต้นใช้งาน")
else:
    try:
        client = genai.Client(api_key=api_key)
        
        # 1.1 & 1.2: Input Event & Location
        st.header("1. ระบุเหตุการณ์และสถานที่")
        
        # ใช้ st.columns เพื่อสร้าง Input 2 ช่องคู่กัน
        col1, col2 = st.columns(2)
        
        event_type = col1.text_input(
            "1.1 ประเภทภัยพิบัติ (เช่น น้ำท่วม, แผ่นดินไหว)",
            value="แผ่นดินไหว",
        )
        location = col2.text_input(
            "1.2 สถานที่ (เช่น กรุงเทพ, ญี่ปุ่น, ทั้งโลก)",
            value="ญี่ปุ่น",
        )
        
        # 2. Processing Button
        if st.button("🚀 สรุปสถิติและวิเคราะห์", type="primary"):
            if event_type and location:
                
                # --- ขั้นตอนที่ 2: สกัดข้อมูลด้วย LLM และ JSON Schema ---
                with st.spinner(f"⏳ กำลังค้นหาสถิติของ '{event_type}' ใน '{location}' และสร้าง JSON..."):
                    
                    system_prompt, user_prompt = create_summary_prompt(event_type, location)
                    
                    response = client.models.generate_content(
                        model='gemini-2.5-flash',
                        contents=[user_prompt],
                        config=types.GenerateContentConfig(
                            system_instruction=system_prompt,
                            response_mime_type="application/json", # บังคับ Output เป็น JSON
                            response_schema=types.Schema(**JSON_SCHEMA),
                            tools=[{"google_search": {}}] # ใช้ Google Search Tool ในการค้นหาข้อมูล
                        )
                    )
                    
                    json_output = response.text.strip()

                if json_output:
                    
                    # --- ขั้นตอนที่ 3: แปลง JSON เป็น Pandas DataFrame และแสดงผล ---
                    st.header("2. ตารางสรุปสถิติเหตุการณ์")
                    
                    try:
                        data: List[Dict[str, Any]] = json.loads(json_output)
                        
                        if not data:
                            st.error(f"❌ ไม่พบข้อมูลสถิติเหตุการณ์ '{event_type}' ใน '{location}' ที่มีข้อมูลครบถ้วน")
                            st.warning("โปรดลองระบุสถานที่หรือประเภทภัยพิบัติที่กว้างขึ้น เช่น 'แผ่นดินไหว' ใน 'ทั้งโลก'")
                            st.subheader("Raw JSON Output (สำหรับตรวจสอบ)")
                            st.code(json_output)
                            st.stop()
                            
                        df = pd.DataFrame(data)
                        
                        # ทำความสะอาดข้อมูลวันที่และเรียงลำดับ
                        df['วันที่_หรือ_ช่วงเวลา_Sort'] = pd.to_datetime(df['วันที่_หรือ_ช่วงเวลา'], errors='coerce')
                        
                        # กรองแถวที่ไม่สามารถแปลงเป็นวันที่ได้ (ถ้ามี)
                        df.dropna(subset=['วันที่_หรือ_ช่วงเวลา_Sort'], inplace=True) 
                        
                        # เรียงลำดับจากเหตุการณ์ที่เกิดขึ้นก่อนไปยังเหตุการณ์ที่เกิดขึ้นหลัง
                        df = df.sort_values(by='วันที่_หรือ_ช่วงเวลา_Sort').reset_index(drop=True)
                        
                        # ลบคอลัมน์ที่ใช้เรียงลำดับออกก่อนแสดงผล
                        df.drop(columns=['วันที่_หรือ_ช่วงเวลา_Sort'], inplace=True)
                        
                        st.success(f"✅ สกัดและจัดโครงสร้างข้อมูลสำเร็จ! (จำนวนเหตุการณ์ที่สกัดได้: {len(df)} ครั้ง)")
                        st.dataframe(df, use_container_width=True)
                        
                        # --- ขั้นตอนที่ 4: สร้าง Bar Chart วิเคราะห์ ---
                        st.header("3. กราฟวิเคราะห์สถิติ (Bar Charts)")
                        
                        # สร้างคอลัมน์ใหม่สำหรับชื่อ Bar Chart
                        df['เหตุการณ์_Label'] = df['วันที่_หรือ_ช่วงเวลา'] + ' (' + df['ลำดับ_เหตุการณ์'].astype(str) + ')'
                        
                        # 4.1 กราฟมูลค่าความเสียหาย
                        fig1 = px.bar(
                            df, 
                            x='เหตุการณ์_Label', 
                            y='มูลค่า_ความเสียหาย_บาท', 
                            title='📈 มูลค่าความเสียหายรวม (บาท) ในแต่ละเหตุการณ์',
                            labels={'มูลค่า_ความเสียหาย_บาท': 'มูลค่าความเสียหาย (บาท)', 'เหตุการณ์_Label': 'วันที่/ช่วงเวลาเหตุการณ์'},
                            height=400,
                            color='มูลค่า_ความเสียหาย_บาท',
                            color_continuous_scale=px.colors.sequential.Sunset
                        )
                        st.plotly_chart(fig1, use_container_width=True)
                        
                        # 4.2 กราฟจำนวนผู้เสียชีวิต
                        fig2 = px.bar(
                            df, 
                            x='เหตุการณ์_Label', 
                            y='ผู้เสียชีวิต_จำนวน', 
                            title='💔 จำนวนผู้เสียชีวิต (คน) ในแต่ละเหตุการณ์',
                            labels={'ผู้เสียชีวิต_จำนวน': 'จำนวนผู้เสียชีวิต (คน)', 'เหตุการณ์_Label': 'วันที่/ช่วงเวลาเหตุการณ์'},
                            height=400,
                            color='ผู้เสียชีวิต_จำนวน',
                            color_continuous_scale=px.colors.sequential.Reds
                        )
                        st.plotly_chart(fig2, use_container_width=True)
                        
                        # 4.3 กราฟจำนวนผู้บาดเจ็บ
                        fig3 = px.bar(
                            df, 
                            x='เหตุการณ์_Label', 
                            y='ผู้บาดเจ็บ_จำนวน', 
                            title='🩹 จำนวนผู้ได้รับบาดเจ็บ (คน) ในแต่ละเหตุการณ์',
                            labels={'ผู้บาดเจ็บ_จำนวน': 'จำนวนผู้บาดเจ็บ (คน)', 'เหตุการณ์_Label': 'วันที่/ช่วงเวลาเหตุการณ์'},
                            height=400,
                            color='ผู้บาดเจ็บ_จำนวน',
                            color_continuous_scale=px.colors.sequential.Blues
                        )
                        st.plotly_chart(fig3, use_container_width=True)
                        
                    except json.JSONDecodeError:
                        st.error("❌ เกิดข้อผิดพลาดในการประมวลผล JSON")
                        st.warning("สาเหตุ: LLM ไม่สามารถสร้าง JSON ที่ถูกต้องตาม Schema ได้")
                        st.subheader("Raw JSON Output (สำหรับตรวจสอบ)")
                        st.code(json_output)
                    except Exception as e:
                        st.error(f"เกิดข้อผิดพลาดในการประมวลผลข้อมูลหรือสร้างกราฟ: {e}")
                else:
                    st.warning("ไม่สามารถสร้าง Output จาก Gemini API ได้")
            else:
                st.warning("โปรดระบุประเภทภัยพิบัติและสถานที่")
                
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาดในการเริ่มต้น Client: {e}")